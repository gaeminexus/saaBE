# G51 — GarantiaRealG51

> ⚠️ Si se modifica la lógica de este reporte, actualizar este archivo.

**Tabla destino:** `RPR.CG51`
**Servicio:** `GeneracionG51ServiceImpl.java`
**Fuente:** `RPR.CG46` (NuevoPrestamoG46) del mismo EJRC

## Lógica
Por cada registro del G46 del período actual → insertar un registro en G51. Si G46 está vacío → retornar 0, EJRD OK.

## Campos

| Campo | Valor |
|---|---|
| `tipoIdentificacion` | `CG46.tipoIdentificacion` |
| `identificacion` | `CG46.identificacion` |
| `numeroOperacion` | `CG46.numeroOperacion` |
| `numeroGarantia` | `CG46.numeroOperacion` (igual al número de operación) |
| `tipoGarantia` | **FIJO: `"A21"`** *(TODO: cambiar para productos cuyo nombre empiece con H)* |
| `descripcionGarantia` | **FIJO: `"Cuenta Individual"`** |
| `valorAvaluo` | **FIJO: `0`** |
| `fechaAvaluo` | `null` |
| `numeroRegistroGarantia` | `null` |
| `fechaContabilizacion` | `null` |
| `porcentajeCubre` | **FIJO: `100`** |
| `estadoRegistro` | **FIJO: `"N"`** |

## Historial de cambios
| Fecha | Cambio |
|---|---|
| — | Sin cambios desde implementación inicial |
